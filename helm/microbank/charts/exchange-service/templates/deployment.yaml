apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "exchange-service.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "exchange-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "exchange-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "exchange-service.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8085"
        prometheus.io/path: "/metrics"
        {{- if .Values.global.serviceMesh.enabled }}
        sidecar.istio.io/inject: "true"
        {{- end }}
    spec:
      containers:
        - name: exchange-service
          image: "{{ .Values.global.imageRegistry }}/microbank-exchange-service:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 8085
              name: http
          env:
            - name: PORT
              value: "8085"
            {{- if .Values.global.otel.enabled }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.global.otel.endpoint | quote }}
            - name: OTEL_SERVICE_NAME
              value: "exchange-service"
            {{- end }}
            - name: RATES_API_URL
              value: {{ .Values.rates.apiUrl | quote }}
            - name: RATES_CACHE_TTL_SECONDS
              value: {{ .Values.rates.cacheTtlSeconds | quote }}
            - name: RATES_API_TIMEOUT_SECONDS
              value: {{ .Values.rates.apiTimeoutSeconds | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8085
            initialDelaySeconds: 3
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
