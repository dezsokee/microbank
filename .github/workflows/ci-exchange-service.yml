name: CI — Exchange Rate Service

on:
  push:
    branches: [main]
    paths: [services/exchange-service/**, .github/workflows/reusable-python.yml, .github/workflows/ci-exchange-service.yml]
  pull_request:
    paths: [services/exchange-service/**, .github/workflows/reusable-python.yml, .github/workflows/ci-exchange-service.yml]
  workflow_dispatch:
    inputs:
      cosign_enabled:
        description: 'Enable Cosign signing and verification'
        type: boolean
        default: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  ci:
    uses: ./.github/workflows/reusable-python.yml
    with:
      service_name: exchange-service
      service_path: services/exchange-service
      cosign_enabled: ${{ github.event_name != 'workflow_dispatch' || inputs.cosign_enabled }}
